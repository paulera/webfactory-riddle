install ImageMagick

convert wonderland.png wonderland.txt

edit wonderland.txt

replace
 \([0-9]+,[0-9]+,[0-9]+,[0-9]+\)  #(......)FF  .*$
by
 \1

replace
^0,
by
\n0,

replace
[0-9]+,[0-9]+:
by
empty

replace
([^\n]+)\n
by
\1


break the text so you have a loooooooong string for each row. dont concatenate rows, make sure it is clear that there is more than one otherwise it will be too hard

